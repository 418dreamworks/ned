o<nudge_c> sub
; Relative nudge of C by #1 degrees, then restore absolute (G90).
; Separate lines for G91/G90 avoid the "same modal group" MDI error.
; (Physical motion still requires the head enabled: SEN high + /S-ON.)
G91
G0 C#1
G90
o<nudge_c> endsub
