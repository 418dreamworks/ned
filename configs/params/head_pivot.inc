# head pivot fed to arm.in0 at HAL load (run5 bakes it into the tcp postgui).
# OPERATOR BANK 2026-08-06 10:24:32: median of 89 per-pair estimates across every
# +-35 data set of 2026-08-05/06 (each pair even-component inverted
# through the leverage; odd/zero drift cancels). sd 0.106, se 0.011.
# Tool 164.8485 in force at all measurements; zero = original -0.12 deg.
PIVOT_LENGTH = 155.9256
