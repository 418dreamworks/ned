# Head A/C absolute-encoder ZERO reference (PS).
# Captured at EYEBALL mechanical zero, head servo OFF (tools/groundtruth/pso_read.sh A|C).
# Refine at the true reference (C-perp / A-0) during calibration (task #9).
# Machine position = (live PSO absolute) - PS   (manual 6.12.6: PM = PE - PS).
# PE = multiturn*R + within-turn (R = 2^26). C mt is stored at its CURRENT winding, so a
# lost/extra full turn shows up as ~+/-204 on C mt (one head-turn = 203.7471 motor turns).
[HEAD_ABS_ZERO]
A_MULTITURN = 36
A_WITHIN    = 44350458
C_MULTITURN = -168
C_WITHIN    = 4280673
